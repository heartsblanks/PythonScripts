You are to WRITE INSTRUCTIONS (a short "how to use Copilot for this task" guide), not a plan and not code.

Non-negotiable rules:
- Output. No commentary.
- Source of truth: Use ONLY the user-provided inputs in the "Input" section.
- Missing data: If required details are missing, include clarifying questions at the top (no limit — ask as many as needed to remove ambiguity). If nothing is missing, omit this section and proceed with explicit assumptions instead.
- No secrets: Never request or include real secrets; use placeholders only.
- Language: English.
- Total output ≤ 400 words.

Deliverable (strict format):
1) Clarifying questions — ONLY if required details are missing; otherwise omit.
2) Assumptions (3 bullets max) — ONLY if no clarifying questions were asked; state what was assumed instead.
3) Global Guardrails (shared by both modes below, 5-8 bullets max) — must explicitly include: Scripted Pipeline only (node/stage/try-catch-finally), do NOT convert to Declarative; only use Jenkins steps/plugins explicitly listed in Input — if not listed, instruct Copilot to ask a clarifying question (don't assume); validation; timeouts/retries; try/finally cleanup; artefacts+junit reporting; secrets via credential ID placeholders only (e.g. CRED_ID_PLACEHOLDER), never echoed.
4) Plan Mode Instructions (Copilot)
   - Goal (1 sentence)
   - What Copilot must produce (3-6 bullets max)
5) Implementation Mode Instructions (Copilot)
   - Goal (1 sentence)
   - What Copilot must produce (5-10 bullets max)
6) Done Criteria (copy verbatim from DoD in Input; do not paraphrase)
7) Output minimisation rules (3 bullets max)

Critical constraints:
- Jenkins Groovy MUST be Scripted Pipeline (node/stage/try-catch-finally). Do NOT convert to Declarative.
- Only use Jenkins steps/plugins explicitly listed in Input; if not listed, instruct Copilot to ask a clarifying question (don't assume).
- If Input contains "Engineering guidelines" and "DoD", reference them as the authority and include them verbatim only by quoting from Input (do not paraphrase or summarise them).
- Never include real secrets; use credential ID placeholders like CRED_ID_PLACEHOLDER; never echo secrets.

Input:
- Repo context:
- Target OS/agent labels:
- Allowed Jenkins steps/plugins:
- Build/test tooling:
- Artefacts + reports to publish:
- Parameters required:
- Credentials required (IDs as placeholders):
- Existing shared library? (Y/N, details):
- Existing pipeline tests? (Y/N, details):
- Engineering guidelines to embed (verbatim):
- Definition of Done (DoD) (verbatim):
